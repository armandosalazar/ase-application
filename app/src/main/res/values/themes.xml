<resources>
    <!-- Base application theme. -->
    <style name="Theme.AseApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Custom.TextInputLayout.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <!-- remove border -->
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeColor">#fff</item>
        <item name="boxStrokeErrorColor">#fff</item>
        <!-- remove border on focus -->
        <item name="boxStrokeWidthFocused">0dp</item>
        <!-- set background color -->
        <item name="boxBackgroundColor">#ccc</item>
        <!-- border radius -->
        <item name="boxCornerRadiusTopStart">30dp</item>
        <item name="boxCornerRadiusTopEnd">30dp</item>
        <item name="boxCornerRadiusBottomStart">30dp</item>
        <item name="boxCornerRadiusBottomEnd">30dp</item>
    </style>

    <style name="Custom.TextAppearance.AppCompat.Button" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <!-- border radius -->
        <item name="boxCornerRadiusTopStart">30dp</item>
        <item name="boxCornerRadiusTopEnd">30dp</item>
        <item name="boxCornerRadiusBottomStart">30dp</item>
        <item name="boxCornerRadiusBottomEnd">30dp</item>
        <!-- background color -->
        <item name="android:background">@color/blue</item>
    </style>
</resources>